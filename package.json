{
  "name": "pixel-gen",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html --public-url ./",
    "deploy": "npm run build && git add dist && git commit -am 'Initial dist subtree commit' && git subtree push --prefix dist origin gh-pages"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "parcel": "^1.10.2"
  }
}
