{
  "name": "pixel-gen",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html --public-url ./",
    "favicon": "cp ./favicon.ico ./dist",
    "deploy": "npm run build && npm run favicon && git add dist && git commit -m 'Initial dist subtree commit' && git subtree push --prefix dist origin gh-pages"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "parcel": "^1.10.2"
  }
}
