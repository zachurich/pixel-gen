{
  "name": "pixel-gen",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html --public-url ./",
    "deploy": "npm run build && git add dist && git commit -m 'Initial dist subtree commit' && git subtree push --prefix dist origin gh-pages"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "downloadjs": "^1.4.7"
  },
  "devDependencies": {
    "parcel": "^1.10.2"
  }
}
